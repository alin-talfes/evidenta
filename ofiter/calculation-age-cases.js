(()=>{
  const view=document.getElementById("calculations"),base=document.getElementById("generated-calculation-lab");
  if(!view||!base||document.getElementById("age-calculation-lab"))return;
  const scopeNote=base.querySelector(".generated-calc-scope small");
  if(scopeNote)scopeNote.textContent="Cazurile 60+ sunt generate separat mai jos; evadarea rămâne momentan în speța oficială Arad până la calibrarea exactă a zilelor-limită.";
  const DAY=86400000,esc=value=>typeof escapeHtml==="function"?escapeHtml(String(value??"")):String(value??"");
  const rand=(min,max)=>Math.floor(Math.random()*(max-min+1))+min,pick=arr=>arr[rand(0,arr.length-1)];
  const iso=d=>d.toISOString().slice(0,10),parse=value=>new Date(`${value}T00:00:00Z`),addDays=(d,n)=>new Date(d.getTime()+n*DAY);
  const ro=d=>new Intl.DateTimeFormat("ro-RO",{day:"2-digit",month:"2-digit",year:"numeric",timeZone:"UTC"}).format(d);
  function addCalendar(date,years=0,months=0){const total=(date.getUTCFullYear()+years)*12+date.getUTCMonth()+months,year=Math.floor(total/12),month=((total%12)+12)%12,last=new Date(Date.UTC(year,month+1,0)).getUTCDate();return new Date(Date.UTC(year,month,Math.min(date.getUTCDate(),last)))}
  const sentenceEnd=(start,duration)=>addDays(addCalendar(start,duration.years,duration.months),-1);
  const days=(a,b)=>Math.floor((b-a)/DAY)+1;
  const over10=d=>d.years>10||(d.years===10&&d.months>0);
  const fractionDays=(total,n,d)=>Math.floor(total*n/d);
  const dateFor=(start,target)=>addDays(start,target-1);
  const label=d=>`${d.years} ani${d.months?` și ${d.months} luni`:""}`;
  const lab=document.createElement("section");lab.id="age-calculation-lab";lab.className="generated-calculation-lab";base.after(lab);
  state.ageCalculationStats=state.ageCalculationStats&&typeof state.ageCalculationStats==="object"?state.ageCalculationStats:{};
  let current=null;
  function persist(){try{if(typeof save==="function")save();else localStorage.setItem("evidenta-training",JSON.stringify(state))}catch{}}
  function generate(){
    const start=new Date(Date.UTC(rand(2020,2025),rand(0,11),rand(2,20))),long=Math.random()<.45,duration=long?pick([{years:10,months:1},{years:11,months:0},{years:12,months:6},{years:14,months:0}]):pick([{years:3,months:6},{years:6,months:7},{years:9,months:11},{years:10,months:0}]);
    const age=rand(60,72),birth=new Date(Date.UTC(start.getUTCFullYear()-age,start.getUTCMonth(),start.getUTCDate())),end=sentenceEnd(start,duration),total=days(start,end),longSentence=over10(duration);
    const totalFraction=longSentence?{n:2,d:3,label:"2/3"}:{n:1,d:2,label:"1/2"},minimum=longSentence?{n:1,d:2,label:"1/2"}:{n:1,d:3,label:"1/3"};
    const fraction=fractionDays(total,totalFraction.n,totalFraction.d),min=fractionDays(total,minimum.n,minimum.d),gap=Math.max(1,fraction-min),gained=Math.random()<.5?rand(Math.max(10,Math.floor(gap*.2)),Math.max(20,Math.floor(gap*.75))):gap+rand(5,60),actual=Math.max(min,fraction-gained);
    current={start:iso(start),birth:iso(birth),age,duration,end:iso(end),total,totalFraction:totalFraction.label,fraction,minimum:minimum.label,min,gained,actual,date:iso(dateFor(start,actual))};
    renderCase();
  }
  function hub(){
    const s=state.ageCalculationStats,attempts=Number(s.attempts||0),acc=s.total?Math.round((s.correct||0)/s.total*100):0;
    lab.innerHTML=`<div class="generated-calc-head"><div><p class="eyebrow">CAZ SPECIAL · 60+</p><h2>Fracția specială de liberare condiționată</h2><p>Persoana are deja cel puțin 60 de ani la începutul executării, astfel încât exercițiul izolează strict calculul fracției și plafonul minim efectiv.</p></div><div class="generated-calc-kpi"><strong>${attempts}</strong><span>spețe 60+ verificate</span><b>${attempts?`${acc}% acuratețe`:"fără istoric"}</b></div></div><div class="generated-calc-controls" style="grid-template-columns:1fr"><button id="age-calc-start" class="primary" type="button">Generează speță 60+</button></div>`;
    document.getElementById("age-calc-start").onclick=generate;
  }
  function renderCase(){
    lab.innerHTML=`<div class="generated-calc-head"><div><p class="eyebrow">CAZ SPECIAL · 60+</p><h2>Fracție + zile considerate executate</h2><p>Calculează fără a memora numere de articole.</p></div><button id="age-calc-back" class="secondary" type="button">Închide speța</button></div><div class="generated-calc-facts"><strong>Date</strong><ul><li>Data începerii: <strong>${ro(parse(current.start))}</strong></li><li>Pedeapsa: <strong>${esc(label(current.duration))}</strong></li><li>Data nașterii: <strong>${ro(parse(current.birth))}</strong> — <strong>${current.age} ani</strong> la începutul executării.</li><li>Zile considerate executate: <strong>${current.gained}</strong>.</li></ul></div><form id="age-calc-form" class="generated-calc-form"><label class="calc-answer-field" data-age-wrap="total"><span>Total zile pedeapsă</span><input data-age-field="total" type="number" inputmode="numeric"><small></small></label><label class="calc-answer-field" data-age-wrap="totalFraction"><span>Fracția totală 60+</span><select data-age-field="totalFraction"><option value="">Alege…</option><option>1/2</option><option>2/3</option><option>3/4</option></select><small></small></label><label class="calc-answer-field" data-age-wrap="fraction"><span>Fracția totală în zile</span><input data-age-field="fraction" type="number" inputmode="numeric"><small></small></label><label class="calc-answer-field" data-age-wrap="minimum"><span>Minimul efectiv</span><select data-age-field="minimum"><option value="">Alege…</option><option>1/3</option><option>1/2</option><option>2/3</option></select><small></small></label><label class="calc-answer-field" data-age-wrap="min"><span>Minimul efectiv în zile</span><input data-age-field="min" type="number" inputmode="numeric"><small></small></label><label class="calc-answer-field" data-age-wrap="actual"><span>Zile efectiv necesare după credit</span><input data-age-field="actual" type="number" inputmode="numeric"><small></small></label><label class="calc-answer-field" data-age-wrap="date"><span>Data propozabilă rezultată</span><input data-age-field="date" type="date"><small></small></label><div class="generated-calc-actions"><button class="primary" type="submit">Verifică</button><button id="age-calc-method" class="secondary" type="button">Arată metoda</button></div></form><section id="age-calc-result" class="generated-calc-result hidden" aria-live="polite"></section>`;
    document.getElementById("age-calc-back").onclick=hub;document.getElementById("age-calc-method").onclick=()=>solution(false);document.getElementById("age-calc-form").onsubmit=e=>{e.preventDefault();check()};
  }
  const names=["total","totalFraction","fraction","minimum","min","actual","date"];
  function check(){let correct=0;for(const name of names){const input=document.querySelector(`[data-age-field="${name}"]`),wrap=document.querySelector(`[data-age-wrap="${name}"]`),small=wrap.querySelector("small"),wanted=String(current[name]),ok=String(input.value).trim()===wanted;if(ok)correct++;wrap.classList.toggle("correct",ok);wrap.classList.toggle("wrong",!ok);small.textContent=ok?"Corect":`Corect: ${name==="date"?ro(parse(wanted)):wanted}`}
    const s=state.ageCalculationStats;s.attempts=(Number(s.attempts)||0)+1;s.correct=(Number(s.correct)||0)+correct;s.total=(Number(s.total)||0)+names.length;persist();solution(true,correct);try{updateStreak()}catch{}
  }
  function solution(scored=false,correct=0){const host=document.getElementById("age-calc-result");if(!host)return;host.classList.remove("hidden");host.innerHTML=`${scored?`<div class="generated-calc-score"><span>Rezultat</span><strong>${correct}/${names.length}</strong><b>${Math.round(correct/names.length*100)}%</b></div>`:""}<div class="generated-calc-solution-grid"><div><span>Total zile</span><strong>${current.total}</strong></div><div><span>Fracție 60+</span><strong>${current.totalFraction} = ${current.fraction} zile</strong></div><div><span>Minim efectiv</span><strong>${current.minimum} = ${current.min} zile</strong></div><div><span>Zile considerate executate</span><strong>${current.gained}</strong></div><div><span>Zile efectiv necesare</span><strong>${current.actual}</strong></div><div><span>Data propozabilă</span><strong>${ro(parse(current.date))}</strong></div></div><section class="generated-calc-method"><p class="eyebrow">METODA</p><ol><li>Stabilești fracția specială pentru persoana care a împlinit 60 de ani: 1/2 până la 10 ani, respectiv 2/3 peste 10 ani.</li><li>Transformi pedeapsa în zile și trunchiezi rezultatul fracției la zi întreagă.</li><li>Dacă există zile considerate executate, scazi creditul din fracția totală, dar nu cobori sub minimul efectiv: 1/3 până la 10 ani, respectiv 1/2 peste 10 ani.</li><li>Transformi numărul de zile efectiv necesare în dată calendaristică de la începutul executării.</li></ol><p class="generated-calc-source">Cazurile generate aici folosesc persoane care au deja 60+ ani la începutul executării; situațiile în care vârsta de 60 de ani se împlinește pe parcurs sunt lăsate separat pentru calibrare.</p></section>`;host.scrollIntoView({behavior:"smooth",block:"start"})}
  hub();
})();
