name: Comprehensive audit remediation

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/fix-full-audit.yml'

permissions:
  contents: write

jobs:
  remediate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Apply audited fixes
        shell: bash
        run: |
          python3 <<'PY'
          from pathlib import Path
          import re, json

          def read(p): return Path(p).read_text(encoding='utf-8')
          def write(p, s):
              Path(p).parent.mkdir(parents=True, exist_ok=True)
              Path(p).write_text(s, encoding='utf-8')

          # ---------- utils.js: safe calendar arithmetic + shared HTML escaping ----------
          p = Path('js/utils.js'); s = read(p)
          s, n = re.subn(r"function addCalendarSafe\(date, years, months, days\) \{.*?\n\}", '''function addCalendarSafe(date, years, months, days) {
    const d = new Date(date);
    if (isNaN(d)) return new Date(NaN);
    const originalDay = d.getDate();
    d.setDate(1);
    d.setFullYear(d.getFullYear() + Number(years || 0));
    d.setMonth(d.getMonth() + Number(months || 0));
    const maxDay = new Date(d.getFullYear(), d.getMonth() + 1, 0).getDate();
    d.setDate(Math.min(originalDay, maxDay));
    d.setDate(d.getDate() + Number(days || 0));
    return d;
}''', s, count=1, flags=re.S)
          if n != 1: raise SystemExit('addCalendarSafe replacement failed')
          if 'function escapeHtml(' not in s:
              s += '''\n\n/** Escapează text pentru inserare sigură în HTML. */\nfunction escapeHtml(value) {\n    return String(value ?? '')\n        .replace(/&/g, '&amp;')\n        .replace(/</g, '&lt;')\n        .replace(/>/g, '&gt;')\n        .replace(/\"/g, '&quot;')\n        .replace(/'/g, '&#39;');\n}\n'''
          write(p, s)

          # ---------- theme.js: one theme implementation + browser chrome color ----------
          p = Path('js/theme.js'); s = read(p)
          s = s.replace("        button.setAttribute('title', isLight ? 'Temă întunecată' : 'Temă luminoasă');", "        button.setAttribute('title', isLight ? 'Temă întunecată' : 'Temă luminoasă');\n        let meta = document.querySelector('meta[name=\"theme-color\"]');\n        if (!meta) { meta = document.createElement('meta'); meta.name = 'theme-color'; document.head.appendChild(meta); }\n        meta.content = isLight ? '#ffffff' : '#061426';")
          write(p, s)

          # ---------- storage.js: remove duplicate theme controller ----------
          p = Path('js/storage.js'); s = read(p)
          s, n = re.subn(r"/\*\*\n \* Aplică tema salvată.*?function toggleTheme\(\) \{.*?\n\}\n\n", '', s, count=1, flags=re.S)
          if n != 1: raise SystemExit('duplicate theme removal failed')
          write(p, s)

          # ---------- app.js: validation, work-day floor, safe rendering, consistent snapshots ----------
          p = Path('js/app.js'); s = read(p)
          old = """function validateInputs(birthDate, startDate, life, art, y, m, d) {\n    const err = [];\n    if (!birthDate) err.push('Data nașterii este invalidă sau incompletă.');\n    if (!startDate) err.push('Data începerii este invalidă sau incompletă.');\n    if (!life && !art) err.push('Selectați articolul de liberare condiționată.');\n    if (!life && y === 0 && m === 0 && d === 0) err.push('Introduceți durata pedepsei.');"""
          new = """function validateInputs(birthDate, startDate, life, art, y, m, d) {\n    const err = [];\n    if (!birthDate) err.push('Data nașterii este invalidă sau incompletă.');\n    if (!startDate) err.push('Data începerii este invalidă sau incompletă.');\n    if (!life && !art) err.push('Selectați articolul de liberare condiționată.');\n    for (const [label, value] of [['Ani', y], ['Luni', m], ['Zile', d]]) {\n        if (!Number.isSafeInteger(value) || value < 0) err.push(`${label}: introduceți un număr întreg pozitiv sau zero.`);\n    }\n    if (!life && y === 0 && m === 0 && d === 0) err.push('Introduceți durata pedepsei.');"""
          if old not in s: raise SystemExit('validate anchor missing')
          s = s.replace(old, new, 1)

          s, n = re.subn(r"function updateProposedDateWithWorkDays\(\) \{.*?\n\}", '''function updateProposedDateWithWorkDays() {
    const input = document.getElementById('workDaysInput');
    const result = document.getElementById('workDaysResult');
    const note = document.getElementById('workDaysNote');
    if (!input || !result || !window.lastProposedDate || !window.lastMinimumDate) return;

    const requested = Number(input.value || 0);
    const safeRequested = Number.isSafeInteger(requested) && requested >= 0 ? requested : 0;
    const maxReduction = Math.max(0, daysBetween(window.lastMinimumDate, window.lastProposedDate));
    const applied = Math.min(safeRequested, maxReduction);
    const newDate = new Date(window.lastProposedDate);
    newDate.setDate(newDate.getDate() - applied);
    result.value = fmtDate(newDate);

    if (note) {
        note.textContent = safeRequested > maxReduction
            ? `Au fost aplicate maximum ${maxReduction} zile; fracția minimă obligatorie (${fmtDate(window.lastMinimumDate)}) nu poate fi depășită.`
            : `Se pot scădea cel mult ${maxReduction} zile până la fracția minimă obligatorie.`;
    }
    input.setCustomValidity(Number.isSafeInteger(requested) && requested >= 0 ? '' : 'Introduceți un număr întreg pozitiv sau zero.');

    if (window.lastCalculation) {
        window.lastCalculation.workDaysRequested = safeRequested;
        window.lastCalculation.workDaysApplied = applied;
        window.lastCalculation.workDaysResult = result.value;
    }
}''', s, count=1, flags=re.S)
          if n != 1: raise SystemExit('work days replacement failed')

          s = s.replace("    const y = parseInt(document.getElementById('durYears').value) || 0;\n    const m = parseInt(document.getElementById('durMonths').value) || 0;\n    const d = parseInt(document.getElementById('durDays').value) || 0;", "    const y = Number(document.getElementById('durYears').value || 0);\n    const m = Number(document.getElementById('durMonths').value || 0);\n    const d = Number(document.getElementById('durDays').value || 0);")
          s = s.replace("    window.lastProposedDate = new Date(tDate);", "    window.lastProposedDate = new Date(tDate);\n    window.lastMinimumDate = new Date(mDate);")
          s = s.replace("    const executedDays = Math.max(0, daysBetween(startDate, today()) + 1);", "    const elapsedEnd = realExp && today() > realExp ? realExp : today();\n    const calendarDaysSinceStart = elapsedEnd >= startDate ? Math.max(0, daysBetween(startDate, elapsedEnd) + 1) : 0;")
          s = s.replace("if (nume) html += `<div class=\"result-item\" style=\"margin-bottom:8px;\"><div class=\"result-label\">Observații</div><div class=\"result-value\">${nume}</div></div>`;", "if (nume) html += `<div class=\"result-item observations-result\"><div class=\"result-label\">Observații</div><div class=\"result-value\">${escapeHtml(nume)}</div></div>`;")
          s = s.replace('<div class="result-item"><div class="result-label">Zile adăugate (neexecutate)</div><div class="result-value" style="color:#ff6b6b;">${non} zile</div></div>', '<div class="result-item"><div class="result-label">Zile adăugate (neexecutate)</div><div class="result-value">${non} zile</div></div>')
          s = s.replace('<div class="result-item"><div class="result-label">Zile executate efectiv</div><div class="result-value">${executedDays} zile</div></div>', '<div class="result-item"><div class="result-label">Zile calendaristice de la începere</div><div class="result-value">${calendarDaysSinceStart} zile</div></div>')
          s = s.replace(' / cu deduceri: ${daysBetween(startDate, mDate) + 1}z', ' / după deduceri și perioade adăugate: ${daysBetween(startDate, mDate) + 1}z')
          s = s.replace(' / cu deduceri: ${daysBetween(startDate, tDate) + 1}z', ' / după deduceri și perioade adăugate: ${daysBetween(startDate, tDate) + 1}z')
          s = s.replace(' / cu deduceri: ${daysBetween(startDate, fDate) + 1}z', ' / după deduceri și perioade adăugate: ${daysBetween(startDate, fDate) + 1}z')
          s = s.replace('<input type="number" id="workDaysInput" min="0" value="0" oninput="updateProposedDateWithWorkDays()">', '<input type="number" id="workDaysInput" min="0" step="1" value="0" oninput="updateProposedDateWithWorkDays()">')
          s = s.replace('<input type="text" id="workDaysResult" readonly style="background:rgba(201,162,39,0.08);font-weight:600;" tabindex="-1">', '<input type="text" id="workDaysResult" readonly class="result-input" tabindex="-1">')
          s = s.replace('<p style="margin-top:8px;font-size:0.75rem;color:var(--text-light);">\n            <em>Scăderea se face direct din data propozabilă (data se reduce cu zilele introduse).</em>\n        </p>', '<p id="workDaysNote" class="help-text"><em>Zilele câștigate reduc data propozabilă, fără a putea coborî sub fracția minimă obligatorie.</em></p>')

          anchor = """        fDate,\n        workDaysInput: document.getElementById('workDaysInput')?.value || '0',"""
          replacement = """        fDate,\n        mDate,\n        duration: { y, m, d },\n        art,\n        inputData: {\n            sex: currentSex === 'M' ? 'Masculin' : 'Feminin',\n            birthDate: document.getElementById('birthDate').value.trim(),\n            observations: document.getElementById('observations').value.trim(),\n            life, art, y: String(y), m: String(m), d: String(d),\n            start: document.getElementById('startDate').value.trim(),\n            condRelease: document.getElementById('conditionalReleaseDate').value.trim(),\n            dedRows: Array.from(document.querySelectorAll('.deduction-row')).map(r => ({ start: r.querySelector('.ded-start')?.value.trim() || '', end: r.querySelector('.ded-end')?.value.trim() || '' })).filter(r => r.start || r.end),\n            manDed: Array.from(document.querySelectorAll('.manual-days')).map(i => i.value),\n            nonRows: Array.from(document.querySelectorAll('.non-exec-row')).map(r => ({ type: r.querySelector('.ne-type')?.value || '', start: r.querySelector('.ne-start')?.value.trim() || '', end: r.querySelector('.ne-end')?.value.trim() || '' })).filter(r => r.start || r.end)\n        },\n        workDaysInput: document.getElementById('workDaysInput')?.value || '0',"""
          if anchor not in s: raise SystemExit('lastCalculation anchor missing')
          s = s.replace(anchor, replacement, 1)

          # Remove duplicate today listeners; HTML handlers remain.
          s = re.sub(r"\n    document\.querySelectorAll\('\.btn-today'\)\.forEach\(btn => \{.*?\n    \}\);\n", '\n', s, count=1, flags=re.S)
          # Accessible modal close path.
          s = s.replace("            const overlay = document.querySelector('.modal-overlay');\n            if (overlay) overlay.remove();", "            const overlay = document.querySelector('.modal-overlay');\n            if (overlay) (typeof closeModal === 'function' ? closeModal(overlay) : overlay.remove());")
          # Debounced true autosave.
          insert = """\n    let autoSaveTimer;\n    const scheduleAutoSave = () => {\n        clearTimeout(autoSaveTimer);\n        autoSaveTimer = setTimeout(autoSave, 250);\n    };\n    document.addEventListener('input', scheduleAutoSave);\n    document.addEventListener('change', scheduleAutoSave);\n"""
          marker = "    // Avertisment la părăsirea paginii\n"
          if marker not in s: raise SystemExit('autosave marker missing')
          s = s.replace(marker, insert + '\n' + marker, 1)
          write(p, s)

          # ---------- export.js: one escape helper, immutable calculation snapshot ----------
          p = Path('js/export.js'); s = read(p)
          s = re.sub(r"/\*\*\n \* Escapează text.*?function escapeHtml\(str\) \{.*?\n\}\n\n", '', s, count=1, flags=re.S)
          s = re.sub(r"        const src = calc\.duration \|\| \{.*?\n            \};", "        const src = calc.duration || { y: 0, m: 0, d: 0 };", s, count=1, flags=re.S)
          s = s.replace("        const art = document.getElementById('liberationArticle').value;", "        const art = calc.art || '';")
          s = s.replace("        let dedPeriodsDays = 0;", "        const dedPeriodsDays = sumIntervals(dedIntervals);")
          s = s.replace("            dedPeriodsDays += days;\n", "")
          marker = "function getInputData() {\n"
          if marker not in s: raise SystemExit('getInputData missing')
          s = s.replace(marker, "function getInputData() {\n    if (window.lastCalculation?.inputData) return JSON.parse(JSON.stringify(window.lastCalculation.inputData));\n", 1)
          write(p, s)

          # ---------- ui.js: accessible dialogs + safe saved-case rendering ----------
          p = Path('js/ui.js'); s = read(p)
          helper = r'''
let modalReturnFocus = null;
function closeModal(overlay) {
    if (!overlay) return;
    overlay.remove();
    if (modalReturnFocus && typeof modalReturnFocus.focus === 'function') modalReturnFocus.focus();
    modalReturnFocus = null;
}
function mountAccessibleModal(overlay) {
    modalReturnFocus = document.activeElement;
    document.body.appendChild(overlay);
    const getFocusable = () => Array.from(overlay.querySelectorAll('button, [href], input, select, textarea, [tabindex]:not([tabindex="-1"])')).filter(el => !el.disabled && el.offsetParent !== null);
    const focusable = getFocusable();
    (focusable[0] || overlay).focus?.();
    overlay.addEventListener('click', e => { if (e.target === overlay) closeModal(overlay); });
    overlay.addEventListener('keydown', e => {
        if (e.key === 'Escape') { e.preventDefault(); closeModal(overlay); return; }
        if (e.key !== 'Tab') return;
        const items = getFocusable();
        if (!items.length) { e.preventDefault(); return; }
        const first = items[0], last = items[items.length - 1];
        if (e.shiftKey && document.activeElement === first) { e.preventDefault(); last.focus(); }
        else if (!e.shiftKey && document.activeElement === last) { e.preventDefault(); first.focus(); }
    });
    overlay.querySelectorAll('.close-btn').forEach(btn => btn.addEventListener('click', () => closeModal(overlay)));
}
'''
          if 'function mountAccessibleModal' not in s:
              s = helper + '\n' + s
          s = s.replace("onclick=\"this.closest('.modal-overlay').remove()\"", '')
          s = s.replace("    document.body.appendChild(overlay);\n    overlay.addEventListener('click', function(e) {\n        if (e.target === overlay) overlay.remove();\n    });", "    mountAccessibleModal(overlay);")
          # occurs for info and legal; replace all remaining exact blocks
          s = s.replace("    document.body.appendChild(overlay);\n    overlay.addEventListener('click', function(e) {\n        if (e.target === overlay) overlay.remove();\n    });", "    mountAccessibleModal(overlay);")
          # Replace openLoadModal entirely with DOM-safe implementation.
          s, n = re.subn(r"function openLoadModal\(\) \{.*?\n\}\n\n/\*\*\n \* Formatează o dată", r'''function openLoadModal() {
    const existingOverlay = document.querySelector('.modal-overlay');
    if (existingOverlay) closeModal(existingOverlay);
    const cases = getCases();
    const names = Object.keys(cases);
    if (names.length === 0) { alert('Nicio speță salvată.'); return; }

    const overlay = document.createElement('div');
    overlay.className = 'modal-overlay';
    overlay.setAttribute('role', 'dialog');
    overlay.setAttribute('aria-modal', 'true');
    overlay.setAttribute('aria-labelledby', 'modal-title');
    overlay.tabIndex = -1;

    const modal = document.createElement('div'); modal.className = 'modal';
    const title = document.createElement('h4'); title.id = 'modal-title'; title.textContent = 'Selectează sau șterge o speță'; modal.appendChild(title);
    const help = document.createElement('p'); help.className = 'help-text'; help.textContent = 'Selectează speța pentru a o încărca.'; modal.appendChild(help);
    const list = document.createElement('ul'); list.setAttribute('role', 'list');
    names.forEach(name => {
        const li = document.createElement('li'); li.className = 'saved-case-row';
        const load = document.createElement('button'); load.type = 'button'; load.className = 'saved-case-name'; load.textContent = name; load.addEventListener('click', () => window.loadCaseByName(name));
        const actions = document.createElement('div'); actions.className = 'saved-case-actions';
        const edit = document.createElement('button'); edit.type = 'button'; edit.className = 'btn btn-outline btn-sm'; edit.textContent = 'Edit'; edit.setAttribute('aria-label', `Redenumește speța ${name}`); edit.addEventListener('click', () => window.renameCase(name));
        const del = document.createElement('button'); del.type = 'button'; del.className = 'btn btn-danger btn-sm'; del.textContent = 'X'; del.setAttribute('aria-label', `Șterge speța ${name}`); del.addEventListener('click', () => window.deleteCase(name));
        actions.append(edit, del); li.append(load, actions); list.appendChild(li);
    });
    modal.appendChild(list);
    const close = document.createElement('button'); close.type = 'button'; close.className = 'btn btn-outline close-btn'; close.textContent = 'Închide'; modal.appendChild(close);
    overlay.appendChild(modal);
    mountAccessibleModal(overlay);
}

/**
 * Formatează o dată''', s, count=1, flags=re.S)
          if n != 1: raise SystemExit('openLoadModal replacement failed')
          write(p, s)

          # ---------- Pure termene core ----------
          termene_core = r'''(function (root) {
'use strict';
function cloneDate(d){ return new Date(d.getTime()); }
function dateOnly(input){ const d = new Date(input); if (isNaN(d)) throw new Error('Dată invalidă.'); return new Date(d.getFullYear(), d.getMonth(), d.getDate()); }
function addDays(date,n){ const d=cloneDate(date); d.setDate(d.getDate()+n); return d; }
function addMonths(date,n){ const d=cloneDate(date), day=d.getDate(); d.setDate(1); d.setMonth(d.getMonth()+n); d.setDate(Math.min(day,new Date(d.getFullYear(),d.getMonth()+1,0).getDate())); return d; }
function addYears(date,n){ const d=cloneDate(date), day=d.getDate(), month=d.getMonth(); d.setDate(1); d.setFullYear(d.getFullYear()+n); d.setMonth(month); d.setDate(Math.min(day,new Date(d.getFullYear(),month+1,0).getDate())); return d; }
function key(date){ return `${date.getFullYear()}-${String(date.getMonth()+1).padStart(2,'0')}-${String(date.getDate()).padStart(2,'0')}`; }
function orthodoxEasterSunday(year){ const a=year%4,b=year%7,c=year%19,d=(19*c+15)%30,e=(2*a+4*b-d+34)%7; const month=Math.floor((d+e+114)/31),day=((d+e+114)%31)+1; const j=new Date(year,month-1,day); const offset=Math.floor(year/100)-Math.floor(year/400)-2; j.setDate(j.getDate()+offset); return j; }
function getRomanianPublicHolidays(year){ const fixed=[[1,1],[1,2],[1,6],[1,7],[1,24],[5,1],[6,1],[8,15],[11,30],[12,1],[12,25],[12,26]]; const out=new Set(fixed.map(([m,d])=>key(new Date(year,m-1,d)))); const easter=orthodoxEasterSunday(year); [-2,0,1,49,50].forEach(delta=>out.add(key(addDays(easter,delta)))); return out; }
function isWeekend(date){ const day=date.getDay(); return day===0||day===6; }
function isHoliday(date){ return getRomanianPublicHolidays(date.getFullYear()).has(key(date)); }
function isNonWorkingDay(date){ return isWeekend(date)||isHoliday(date); }
function nextWorkingDay(date){ let d=cloneDate(date); while(isNonWorkingDay(d)) d=addDays(d,1); return d; }
function calculateDeadline({start,duration,unit,termType='general'}){
  if(!['hours','days','months','years'].includes(unit)) throw new Error('Unitate invalidă.');
  if(!Number.isSafeInteger(duration)||duration<=0) throw new Error('Durata trebuie să fie un număr întreg pozitiv.');
  const preventive=termType==='preventive';
  if(unit==='hours'){
    const d=new Date(start); if(isNaN(d)) throw new Error('Data/ora de început invalidă.');
    const raw=new Date(d); raw.setHours(raw.getHours()+(preventive?duration-1:duration+1));
    return {rawDeadline:raw,deadline:raw,effectiveDeadline:raw,rule:preventive?'art. 271':'art. 269',workingDayAdjustment:false};
  }
  const d=dateOnly(start); let raw;
  if(unit==='days') raw=addDays(d,preventive?duration-1:duration+1);
  else if(unit==='months') raw=preventive?addDays(addMonths(d,duration),-1):addMonths(d,duration);
  else raw=preventive?addDays(addYears(d,duration),-1):addYears(d,duration);
  let deadline=raw, adjusted=false;
  if(!preventive && isNonWorkingDay(raw)){ deadline=nextWorkingDay(raw); adjusted=true; }
  return {rawDeadline:raw,deadline,effectiveDeadline:deadline,rule:preventive?'art. 271':'art. 269',workingDayAdjustment:adjusted};
}
root.TermeneCore={addDays,addMonths,addYears,orthodoxEasterSunday,getRomanianPublicHolidays,isHoliday,isNonWorkingDay,nextWorkingDay,calculateDeadline};
})(typeof window!=='undefined'?window:globalThis);
'''
          write('js/termene-core.js', termene_core)

          # Extract termene page script, then simplify it to use core.
          p = Path('termene.html'); html = read(p)
          m = re.search(r"\n    <script>\n(        // ===== Mascare automată.*)\n    </script>\n", html, flags=re.S)
          if not m: raise SystemExit('termene inline script not found')
          ui = m.group(1)
          ui = re.sub(r"        // ===== Sărbători legale 2026-2028 =====.*?        // ===== Termene predefinite =====", "        const { calculateDeadline } = TermeneCore;\n\n        // ===== Termene predefinite =====", ui, flags=re.S)
          ui = re.sub(r"\n        // ===== Regula specială pentru contestație:.*?\n        function calculateDeadline\(.*?\n        \}\n", "\n", ui, flags=re.S)
          ui = ui.replace("                calcMode: 'working',\n", "")
          ui = ui.replace("                calcMode: 'working',\n", "")
          ui = ui.replace("                calcMode: 'working',\n", "")
          ui = ui.replace("                calcMode: 'working',\n", "")
          ui = ui.replace("            document.getElementById('calcModeSelect').value = preset.calcMode;\n", "")
          ui = ui.replace("            const calcMode = document.getElementById('calcModeSelect').value;\n", "")
          ui = ui.replace("                const res = calculateDeadline({ start, duration, unit, termType, calcMode });", "                const res = calculateDeadline({ start, duration, unit, termType });")
          ui = ui.replace("                let displayDate;\n                if (preset === 'contestatie') {\n                    displayDate = adjustForContestatie(res.effectiveDeadline);\n                } else {\n                    displayDate = res.effectiveDeadline;\n                }", "                const displayDate = res.effectiveDeadline;")
          ui = ui.replace("            if (!str) return { hours: 0, minutes: 0 };", "            if (!str) return { hours: 0, minutes: 0 };")
          ui = ui.replace("            if (isNaN(hours) || isNaN(minutes)) return { hours: 0, minutes: 0 };\n            return { hours, minutes };", "            if (isNaN(hours) || isNaN(minutes) || hours < 0 || hours > 23 || minutes < 0 || minutes > 59) return null;\n            return { hours, minutes };")
          ui = ui.replace("            const { hours, minutes } = parseRoTime(timeStr);\n            d.setHours(hours, minutes, 0, 0);", "            const time = parseRoTime(timeStr);\n            if (!time) return null;\n            d.setHours(time.hours, time.minutes, 0, 0);")
          write('js/termene.js', ui.strip() + '\n')
          html = html[:m.start()] + '\n    <script src="js/termene-core.js?v=34"></script>\n    <script src="js/termene.js?v=34"></script>\n' + html[m.end():]
          html = re.sub(r"\n\s*<label for=\"calcModeSelect\">Mod calcul</label>\s*<select id=\"calcModeSelect\">.*?</select>\s*", '\n', html, count=1, flags=re.S)
          html = html.replace('<option value="contestatie">Contestație – 3 zile</option>', '<option value="contestatie">Contestație în materia executării – 3 zile</option>')
          html = html.replace('<p><em>Pentru contestație, data rămânerii definitive nu poate fi luni sau vineri, indiferent de prelungirile pentru zile nelucrătoare.</em></p>', '<p><em>Termenele pe zile se calculează pe unități libere conform art. 269 alin. (2); dacă ultima zi este nelucrătoare, termenul se prelungește potrivit alin. (4).</em></p>')
          write(p, html)

          # ---------- Pure contopiri core + extracted UI ----------
          cont_core = r'''(function(root){
'use strict';
const DAYS_PER_MONTH=30, MONTHS_PER_YEAR=12, DAYS_PER_YEAR=DAYS_PER_MONTH*MONTHS_PER_YEAR;
function toDays(years,months,days){ for(const v of [years,months,days]) if(!Number.isSafeInteger(v)||v<0) throw new Error('Duratele trebuie să fie numere întregi pozitive sau zero.'); return years*DAYS_PER_YEAR+months*DAYS_PER_MONTH+days; }
function fromDays(totalDays){ if(!Number.isSafeInteger(totalDays)||totalDays<0) throw new Error('Durată totală invalidă.'); const years=Math.floor(totalDays/DAYS_PER_YEAR), remainder=totalDays%DAYS_PER_YEAR, months=Math.floor(remainder/DAYS_PER_MONTH), days=remainder%DAYS_PER_MONTH; return {years,months,days}; }
function formatDuration({years,months,days}){ const p=[]; if(years) p.push(`${years} ani`); if(months) p.push(`${months} luni`); if(days) p.push(`${days} zile`); return p.join(', ')||'0 zile'; }
function calculate(groups){
 const concurs=groups.concurs||[], recidiva=groups.recidiva||[], revocare=groups.revocare||[];
 if(!concurs.length&&!recidiva.length&&!revocare.length) throw new Error('Adaugă cel puțin o pedeapsă validă.');
 let concursResultDays=0, bonusDays=0, maxPenalty=null, othersTotalDays=0;
 if(concurs.length){ maxPenalty=concurs.reduce((max,p)=>p.totalDays>max.totalDays?p:max,concurs[0]); const others=concurs.filter(p=>p!==maxPenalty); othersTotalDays=others.reduce((sum,p)=>sum+p.totalDays,0); bonusDays=Math.floor(othersTotalDays/3); concursResultDays=maxPenalty.totalDays+bonusDays; }
 const recidivaTotalDays=recidiva.reduce((s,p)=>s+p.totalDays,0), revocareTotalDays=revocare.reduce((s,p)=>s+p.totalDays,0);
 const finalDays=concursResultDays+recidivaTotalDays+revocareTotalDays;
 return {finalDays,finalDuration:fromDays(finalDays),concursResultDays,bonusDays,maxPenalty,othersTotalDays,recidivaTotalDays,revocareTotalDays};
}
root.ContopiriCore={DAYS_PER_MONTH,DAYS_PER_YEAR,toDays,fromDays,formatDuration,calculate};
})(typeof window!=='undefined'?window:globalThis);
'''
          write('js/contopiri-core.js', cont_core)
          p = Path('contopiri.html'); html = read(p)
          m = re.search(r"\n    <script>\n(        // ===== Utilitare =====.*)\n    </script>\n", html, flags=re.S)
          if not m: raise SystemExit('contopiri inline script not found')
          ui = m.group(1)
          ui = re.sub(r"        // ===== Utilitare =====.*?        // ===== Gestionare rânduri =====", "        const { toDays, fromDays, formatDuration, calculate } = ContopiriCore;\n\n        // ===== Gestionare rânduri =====", ui, flags=re.S)
          # Replace calculation result assembly while preserving grouping/UI.
          start = ui.find('            let concursResultDays = 0;')
          end = ui.find('            const resultDiv = document.getElementById(\'mergeResult\');', start)
          if start < 0 or end < 0: raise SystemExit('contopiri calculation block missing')
          block = '''            let calculation;\n            try { calculation = calculate({ concurs: concursPenalties, recidiva: recidivaPenalties, revocare: revocarePenalties }); }\n            catch (err) { alert(err.message); return; }\n            const details = [];\n            if (calculation.maxPenalty) {\n                details.push(`Pedeapsa cea mai grea (concurs): ${formatDuration(calculation.maxPenalty)}`);\n                details.push(`Totalul celorlalte pedepse (concurs): ${formatDuration(fromDays(calculation.othersTotalDays))}`);\n                details.push(`Spor 1/3: ${formatDuration(fromDays(calculation.bonusDays))}`);\n                details.push(`Rezultanta concurs: ${formatDuration(fromDays(calculation.concursResultDays))}`);\n            }\n            if (recidivaPenalties.length) details.push(`Pedepse recidivă (adăugate integral): ${recidivaPenalties.map(p => formatDuration(p)).join(' + ')}`);\n            if (revocarePenalties.length) details.push(`Revocare rest (adăugat integral): ${revocarePenalties.map(p => formatDuration(p)).join(' + ')}`);\n            const finalDuration = calculation.finalDuration;\n\n'''
          ui = ui[:start] + block + ui[end:]
          ui = ui.replace("                const years = parseInt(row.querySelector('.penalty-years').value) || 0;\n                const months = parseInt(row.querySelector('.penalty-months').value) || 0;\n                const days = parseInt(row.querySelector('.penalty-days').value) || 0;", "                const years = Number(row.querySelector('.penalty-years').value || 0);\n                const months = Number(row.querySelector('.penalty-months').value || 0);\n                const days = Number(row.querySelector('.penalty-days').value || 0);\n                if (![years, months, days].every(v => Number.isSafeInteger(v) && v >= 0)) { alert('Duratele trebuie să fie numere întregi pozitive sau zero.'); return; }")
          write('js/contopiri.js', ui.strip()+'\n')
          html = html[:m.start()] + '\n    <script src="js/contopiri-core.js?v=34"></script>\n    <script src="js/contopiri.js?v=34"></script>\n' + html[m.end():]
          html = html.replace('Revocare rest:</strong> Restul rămas neexecutat din pedeapsa anterioară se adaugă integral la pedeapsa rezultantă finală, fără a fi transformat în luni sau ani (se adună direct la zile).', 'Revocare rest:</strong> Restul rămas neexecutat se adaugă integral la cuantumul rezultat; afișarea finală este normalizată în ani, luni și zile, fără a modifica totalul aritmetic.')
          write(p, html)

          # ---------- Extract transfer inline application scripts; business dataset untouched ----------
          for page, out, anchor in [('transfer/index.html','transfer/app.js','(function()'), ('transfer/rules.html','transfer/rules-page.js','(function()')]:
              html = read(page)
              scripts = list(re.finditer(r"\n\s*<script>\s*(.*?)\s*</script>\s*", html, flags=re.S))
              target = None
              for sm in scripts:
                  if anchor in sm.group(1): target = sm
              if target:
                  code = target.group(1).strip()+'\n'
                  write(out, code)
                  rel = out.split('/')[-1]
                  html = html[:target.start()] + f'\n    <script src="{rel}?v=34"></script>\n' + html[target.end():]
                  write(page, html)
          # Mark custodie results explicitly; no business-rule changes.
          p=Path('transfer/rules.js'); s=read(p)
          s=s.replace("matches.push({ unitate: u, regimJudete: judeteList, judeteDeservite: u.judeteDeservite || [] });", "matches.push({ unitate: u, regimJudete: judeteList, judeteDeservite: u.judeteDeservite || [], isCustodie: true });", 1)
          write(p,s)

          # ---------- index version script extraction + semantic HTML ----------
          p=Path('index.html'); html=read(p)
          if not html.lstrip().lower().startswith('<!doctype html>'): html='<!DOCTYPE html>\n'+html
          vm=re.search(r"</head>\s*<script>\s*(fetch\('./version\.json'\).*?)</script>\s*<body>",html,flags=re.S)
          if vm:
              write('js/version.js', "document.addEventListener('DOMContentLoaded', () => {\n"+vm.group(1).strip()+"\n});\n")
              html=html[:vm.start()]+'</head>\n<body>'+html[vm.end():]
              html=html.replace('<script src="js/theme.js?v=33" defer></script>', '<script src="js/theme.js?v=34" defer></script>\n    <script src="js/version.js?v=34" defer></script>')
          write(p,html)

          # ---------- Shared HTML hygiene ----------
          pages=['index.html','termene.html','contopiri.html','transfer/index.html','transfer/rules.html']
          for page in pages:
              html=read(page)
              html=re.sub(r'maximum-scale=1\.0,\s*user-scalable=no,\s*', '', html)
              html=html.replace('css/style.css?v=33','css/style.css?v=34').replace('../css/style.css?v=33','../css/style.css?v=34')
              html=html.replace('js/theme.js?v=33','js/theme.js?v=34').replace('../js/theme.js?v=33','../js/theme.js?v=34')
              html=html.replace('<meta name="theme-color" content="#1f2937">','<meta name="theme-color" content="#061426">')
              write(page,html)

          # ---------- CSS: remove external font, one canonical variable system, no legacy color layer ----------
          p=Path('css/style.css'); css=read(p)
          css=re.sub(r"@import url\([^\n]+\);\s*",'',css,count=1)
          canonical=r''':root {
    color-scheme: dark;
    --bg:#061426; --bg-elevated:#081a30; --card-bg:#0a1d33; --surface-2:#0d2440; --surface-3:#112c4d;
    --primary:#ffffff; --accent:#ffffff; --accent-strong:#ffffff; --accent-soft:rgba(255,255,255,.08);
    --gold:#ffffff; --gold-light:#ffffff; --text:#ffffff; --text-light:rgba(255,255,255,.78); --muted:rgba(255,255,255,.60);
    --border:rgba(255,255,255,.15); --border-strong:rgba(255,255,255,.30); --danger:#ffffff; --warning:#ffffff; --success:#ffffff; --info:#ffffff;
    --input-bg:#081a30; --input-border:rgba(255,255,255,.24); --input-placeholder:rgba(255,255,255,.48); --label-color:#ffffff;
    --radius-sm:8px; --radius-md:12px; --radius-lg:16px; --shadow:0 16px 45px rgba(0,0,0,.22); --shadow-soft:0 7px 24px rgba(0,0,0,.16);
    --font:'Aptos','Segoe UI',system-ui,-apple-system,sans-serif; --font-mono:var(--font); --btn-height:44px; --btn-padding-y:11px; --btn-padding-x:16px;
    --card-padding-y:20px; --card-padding-x:20px; --gap-sm:8px; --gap-md:12px; --gap-lg:18px; --border-width:1px; --outline-width:3px; --transition-fast:.14s; --transition-base:.2s;
}
body.light {
    color-scheme:light; --bg:#ffffff; --bg-elevated:#ffffff; --card-bg:#ffffff; --surface-2:#ffffff; --surface-3:#ffffff;
    --primary:#0b2a4a; --accent:#0b2a4a; --accent-strong:#0b2a4a; --accent-soft:rgba(11,42,74,.07); --gold:#0b2a4a; --gold-light:#0b2a4a;
    --text:#000000; --text-light:#000000; --muted:#0b2a4a; --border:rgba(11,42,74,.22); --border-strong:#0b2a4a;
    --danger:#0b2a4a; --warning:#0b2a4a; --success:#0b2a4a; --info:#0b2a4a; --input-bg:#ffffff; --input-border:rgba(11,42,74,.35); --input-placeholder:rgba(0,0,0,.48); --label-color:#000000;
    --shadow:0 14px 38px rgba(11,42,74,.10); --shadow-soft:0 5px 18px rgba(11,42,74,.08);
}
'''
          css,n=re.subn(r":root \{.*?\n\}\n\nbody\.light \{.*?\n\}\n",canonical+'\n',css,count=1,flags=re.S)
          if n!=1: raise SystemExit('canonical CSS root replacement failed')
          marker='/* ===== UNIVERSAL NAVY / WHITE DESIGN SYSTEM ===== */'
          if marker in css:
              tail=css.split(marker,1)[1]
              # Keep component normalization, remove duplicate variable blocks.
              tail=re.sub(r"\s*:root \{.*?\}\s*body\.light \{.*?\}\s*",'\n',tail,count=1,flags=re.S)
              css=css.split(marker,1)[0].rstrip()+"\n\n/* ===== UNIVERSAL COMPONENT NORMALIZATION ===== */\n"+tail.lstrip()
          replacements={
              '#4da3ff':'var(--accent)', '#79c8ff':'var(--accent-strong)', '#39d6b4':'var(--text)', '#f7bd60':'var(--text)', '#ff7283':'var(--text)',
              '#0b65c8':'#0b2a4a', '#084f9e':'#0b2a4a', '#172235':'#000000', '#52647b':'#000000', '#70829a':'#0b2a4a',
              '#c83f55':'#0b2a4a', '#a66308':'#0b2a4a', '#087d69':'#0b2a4a', '#ff6b6b':'var(--text)', '#c9a227':'var(--accent)', '#e8c95a':'var(--accent)'
          }
          for a,b in replacements.items(): css=css.replace(a,b).replace(a.upper(),b)
          css += '''\n.saved-case-row{display:flex;align-items:center;justify-content:space-between;gap:10px;padding:8px 0;border-bottom:1px solid var(--border)}\n.saved-case-name{flex:1;text-align:left;text-transform:none!important;letter-spacing:0!important;background:transparent!important;border:0!important;color:var(--text)!important;min-height:38px!important}\n.saved-case-actions{display:flex;gap:6px}.observations-result{margin-bottom:8px}.result-input{font-weight:600}.help-text{margin-top:8px;font-size:.75rem;color:var(--text-light)}\n'''
          write(p,css)

          # ---------- Version ----------
          vp=Path('version.json')
          try: data=json.loads(read(vp)); data['version']='0.165'
          except Exception: data={'version':'0.165'}
          write(vp,json.dumps(data,ensure_ascii=False,indent=2)+'\n')

          # ---------- Automated regression tests ----------
          tests=r'''const fs=require('fs'),vm=require('vm'),assert=require('assert');
function load(path,extra=''){ const ctx={console,Date,Math,Number,String,Array,Object,Set,JSON,globalThis:null}; ctx.globalThis=ctx; vm.createContext(ctx); vm.runInContext(fs.readFileSync(path,'utf8')+'\n'+extra,ctx,{filename:path}); return ctx; }
let u=load('js/utils.js',';globalThis.__add=addCalendarSafe;globalThis.__days=daysBetween;');
assert.equal(u.__add(new Date(2025,0,31),0,1,0).getDate(),28); assert.equal(u.__add(new Date(2024,1,29),1,0,0).getDate(),28);
let t=load('js/termene-core.js'); const T=t.TermeneCore;
assert.equal(T.orthodoxEasterSunday(2026).toISOString().slice(0,10),'2026-04-12'); assert(T.getRomanianPublicHolidays(2031).has('2031-12-25'));
let d=T.calculateDeadline({start:new Date(2014,3,15),duration:3,unit:'days',termType:'general'}); assert.equal(d.effectiveDeadline.getFullYear(),2014); assert.equal(d.effectiveDeadline.getMonth(),3); assert.equal(d.effectiveDeadline.getDate(),21);
let p=T.calculateDeadline({start:new Date(2026,0,10),duration:3,unit:'days',termType:'preventive'}); assert.equal(p.effectiveDeadline.getDate(),12);
let c=load('js/contopiri-core.js'); const C=c.ContopiriCore; assert.equal(C.toDays(1,0,0),360); assert.deepEqual(JSON.parse(JSON.stringify(C.fromDays(390))),{years:1,months:1,days:0});
let calc=C.calculate({concurs:[{totalDays:360,years:1,months:0,days:0},{totalDays:180,years:0,months:6,days:0}],recidiva:[],revocare:[{totalDays:30,years:0,months:1,days:0}]}); assert.equal(calc.finalDays,450); assert.deepEqual(JSON.parse(JSON.stringify(calc.finalDuration)),{years:1,months:3,days:0});
let tr=load('transfer/rules.js',';globalThis.__u=UNITATI;globalThis.__g=gasesteUnitati;'); assert(tr.__u.length>=39); assert.equal(new Set(tr.__u.map(x=>x.id)).size,tr.__u.length); assert(Array.isArray(tr.__g('masculin','major','inchis','Hunedoara','executare',false)));
for(const f of ['index.html','termene.html','contopiri.html','transfer/index.html','transfer/rules.html']){ const h=fs.readFileSync(f,'utf8'); assert(/^\s*<!DOCTYPE html>/i.test(h),f+' missing doctype'); assert(!/user-scalable=no/i.test(h),f+' disables zoom'); const inline=[...h.matchAll(/<script(?![^>]*\bsrc=)[^>]*>/gi)]; assert.equal(inline.length,0,f+' contains inline script'); }
const storage=fs.readFileSync('js/storage.js','utf8'); assert(!/function\s+(applyTheme|toggleTheme)\s*\(/.test(storage),'duplicate theme functions');
const css=fs.readFileSync('css/style.css','utf8'); assert(!/fonts\.googleapis\.com/.test(css),'external font import'); assert.equal((css.match(/\/\* ===== UNIVERSAL COMPONENT NORMALIZATION ===== \*\//g)||[]).length,1);
console.log('All audit regression tests passed.');
'''
          write('tests/run-tests.js',tests)
          write('package.json',json.dumps({'name':'evidenta-ppl','private':True,'scripts':{'test':'node tests/run-tests.js','check:syntax':'find js transfer -name "*.js" -print0 | xargs -0 -n1 node --check'}},indent=2)+'\n')
          ci='''name: CI\n\non:\n  push:\n    branches: [main]\n  pull_request:\n\npermissions:\n  contents: read\n\njobs:\n  test:\n    runs-on: ubuntu-latest\n    steps:\n      - uses: actions/checkout@v4\n      - uses: actions/setup-node@v4\n        with:\n          node-version: '22'\n      - run: npm run check:syntax\n      - run: npm test\n'''
          write('.github/workflows/ci.yml',ci)
          PY

      - name: Run syntax and regression checks
        shell: bash
        run: |
          set -euo pipefail
          npm run check:syntax
          npm test
          grep -R "function applyTheme\|function toggleTheme" js/storage.js && exit 1 || true
          ! grep -R "fonts.googleapis.com" css/style.css
          ! grep -R "user-scalable=no" -- *.html transfer/*.html

      - name: Commit remediation
        shell: bash
        run: |
          set -euo pipefail
          rm .github/workflows/fix-full-audit.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Resolve full HTML JavaScript CSS audit findings"
          git push origin main
